{% set name = "jedi-language-server" %}
{% set version = "0.28.6" %}


package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/jedi-language-server-{{ version }}.tar.gz
  sha256: c3aeed7878ccb8a52ba80beeeb8060dade8933cc4a394d9107d669d8c917b8a8

build:
  number: 0
  noarch: python
  entry_points:
    - jedi-language-server = jedi_language_server.cli:cli
  script: {{ PYTHON }} -m pip install . -vv --no-deps

requirements:
  host:
    - pip
    - poetry >=1.0.5
    - python >=3.6.1
  run:
    - click >=7.0
    - docstring-to-markdown >=0.4,<1.0
    - jedi ==0.18.0
    - pydantic >=1.7,<2
    - pygls >=0.10.0,<1
    - python >=3.6.1,<4

test:
  imports:
    - jedi_language_server
  commands:
    - pip check
    - jedi-language-server --version
    - jedi-language-server --help
  requires:
    - pip

about:
  home: https://github.com/pappasam/jedi-language-server
  summary: A language server for Jedi!
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - bollwyvl
